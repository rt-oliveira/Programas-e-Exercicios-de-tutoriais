-- Está ordenando os registros da tabela Customers
-- pelo nome, e em caso de igualdade, pelo salário.
SELECT * FROM Customers ORDER BY name, salary;

-- Está ordenando os registros da tabela Customers
-- pelo nome, de maneira decrescente.
SELECT * FROM Customers ORDER BY name DESC;