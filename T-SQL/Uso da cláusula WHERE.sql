-- Nesta consulta serão mostrados somente aqueles
-- que tiverem salário maior que 2000.
SELECT id, name, salary FROM customers WHERE salary>2000;

-- Já nesta será(ão)  mostrado(s) aqueles que tiverem
-- o nome igual a "Hardik".
SELECT id, name, salary FROM customers WHERE name='Hardik';