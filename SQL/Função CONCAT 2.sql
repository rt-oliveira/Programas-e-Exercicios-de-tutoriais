-- Função CONCAT
-- Dada uma quantidade de strings passadas como parâmetro, ela
-- retornará uma única string, contendo todas as strings passadas
-- concatenadas.

-- Neste exemplo, as string 'My', 'S' e 'QL' vão se tornar uma única
-- string contendo 'MySQL'.
SELECT CONCAT('My','S','QL');
